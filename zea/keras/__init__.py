"""This module provides ``keras`` integration into ``zea``.

Most importantly, ``zea.keras.ops`` contains a auto-generated :class:`zea.Operation` for
all unary functions in ``keras.ops`` and ``keras.ops.image``.
"""

from . import ops
